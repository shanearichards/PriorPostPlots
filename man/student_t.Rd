% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{student_t}
\alias{student_t}
\alias{dstudent_t}
\alias{pstudent_t}
\alias{qstudent_t}
\alias{rstudent_t}
\title{Student-t distribution, Stan's location-scale parameterisation}
\usage{
dstudent_t(x, nu, mu = 0, sigma = 1)

pstudent_t(q, nu, mu = 0, sigma = 1)

qstudent_t(p, nu, mu = 0, sigma = 1)

rstudent_t(n, nu, mu = 0, sigma = 1)
}
\arguments{
\item{x, q}{vector of quantities.}

\item{nu}{degrees of freedom (\code{nu > 0}; \code{nu = 1} is the Cauchy
distribution, and the limit \code{nu -> Inf} is the normal
distribution).}

\item{mu}{location.}

\item{sigma}{scale (\code{sigma > 0}).}

\item{p}{vector of probabilities.}

\item{n}{number of observations.}
}
\value{
Vector of densities (\code{dstudent_t}), probabilities
(\code{pstudent_t}), quantiles (\code{qstudent_t}), or random draws
(\code{rstudent_t}).
}
\description{
Base R's \code{stats::dt()}/\code{pt()}/\code{qt()} only implement the
standard (location 0, scale 1) Student-t distribution. Stan's
\code{student_t(nu, mu, sigma)} adds a location \code{mu} and scale
\code{sigma}; \code{dstudent_t()}/\code{pstudent_t()}/\code{qstudent_t()}/
\code{rstudent_t()} implement that three-argument form directly, via the
standard location-scale transform (\code{(x - mu) / sigma}).
}
\keyword{internal}

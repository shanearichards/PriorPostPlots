% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{extract_posterior_list}
\alias{extract_posterior_list}
\title{Extract posterior draws from a fitted Stan model into the same
shape \code{rstan::extract()} produces - a named list with one entry per
base parameter: a plain vector for a scalar parameter, an
\verb{iterations x J} matrix for a \code{vector[J]} parameter, or an
\verb{iterations x R x C} array for a \code{matrix[R, C]} parameter.}
\usage{
extract_posterior_list(stan_fit, base_pars)
}
\description{
Two input types are handled: an \code{rstan::sampling()}/\code{rstan::stan()}
fit (class \code{"stanfit"}) is passed straight to \code{rstan::extract()},
unchanged from previous versions of this package. Anything else with
a \verb{$draws(variables = ..., format = "matrix")} method - which
includes every \code{cmdstanr} fit class (\code{CmdStanMCMC}, and more broadly
\code{CmdStanFit}) without \code{cmdstanr} needing to be an explicit dependency
of this package - has its \code{draws_matrix} output (column names
\code{"name"}/\code{"name[i]"}/\code{"name[i,j]"}, one column per scalar element)
reshaped into the same vector/matrix/array-per-parameter list
structure by hand, since \code{cmdstanr} has no equivalent of
\code{rstan::extract()}'s automatic per-parameter reshaping.
}
\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{find_lkj_corr_map}
\alias{find_lkj_corr_map}
\title{Scan the \verb{transformed parameters}/\verb{generated quantities} blocks for the
standard Stan idiom that materialises a correlation matrix from a
\code{cholesky_factor_corr} parameter: \verb{<name> = multiply_lower_tri_self_ transpose(<L name>);} (with an optional leading type/size declaration).
Returns a named list keyed by \verb{<L name>}, valued \verb{<name>} - used both to
build \code{parse_lkj_priors()}'s prior rows and, by \code{Create_df_priors()}, to
keep \code{parse_parameters_block()} from separately flagging \verb{<name>} as an
"unsupported type" (\code{corr_matrix}) when it's declared inside
\verb{transformed parameters} rather than \verb{generated quantities}.}
\usage{
find_lkj_corr_map(transformed_parameters_block, generated_quantities_block)
}
\description{
Scan the \verb{transformed parameters}/\verb{generated quantities} blocks for the
standard Stan idiom that materialises a correlation matrix from a
\code{cholesky_factor_corr} parameter: \verb{<name> = multiply_lower_tri_self_ transpose(<L name>);} (with an optional leading type/size declaration).
Returns a named list keyed by \verb{<L name>}, valued \verb{<name>} - used both to
build \code{parse_lkj_priors()}'s prior rows and, by \code{Create_df_priors()}, to
keep \code{parse_parameters_block()} from separately flagging \verb{<name>} as an
"unsupported type" (\code{corr_matrix}) when it's declared inside
\verb{transformed parameters} rather than \verb{generated quantities}.
}
\keyword{internal}

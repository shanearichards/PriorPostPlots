% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{infer_size_from_fit}
\alias{infer_size_from_fit}
\title{A \code{vector[K]}/\code{matrix[R, C]} declaration's size sometimes references a
data-block constant (\code{K}) rather than a literal, and \code{data_list} is the
usual way to resolve that. But Stan itself already had to resolve it to
produce the posterior draws in the first place, so if a fitted model is
available, its own draws are a second, independent source of truth for
a given parameter's true size - a cmdstanr fit's \code{"name[1]"}, \code{"name[2]"},
... column names, or an rstan fit's already-reshaped
\verb{iterations x K} matrix (or \verb{iterations x R x C} array for a matrix
parameter), directly reveal it with no need to know what \code{K} itself
equals. This is tried only as a fallback, after \code{data_list}/a literal
has already failed to resolve the size - see \code{Create_df_priors()}'s
\code{stan_fit} argument.}
\usage{
infer_size_from_fit(stan_fit, pname, kind = c("vector", "matrix"))
}
\description{
\code{kind} is \code{"vector"} (returns a single size, or \code{NA} if it can't be
determined) or \code{"matrix"} (returns \code{c(nrow, ncol)}, each \code{NA}
independently if undetermined). Any failure - the parameter isn't in
the fit, or \code{stan_fit}'s own \verb{$draws()}/\code{rstan::extract()} errors for
any reason - is treated as "couldn't infer", not a hard error, since
this is only ever a best-effort second attempt.
}
\keyword{internal}

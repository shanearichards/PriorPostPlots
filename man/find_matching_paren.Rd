% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{find_matching_paren}
\alias{find_matching_paren}
\title{Find the position of the \verb{)} matching the \code{(} at \code{open_pos} in \code{text},
by scanning forward and tracking parenthesis depth. Handles arbitrary
nesting (e.g. the \verb{)} closing \code{normal(log(0.5), 0.75)}'s outer call).
Returns \code{NA} if the parentheses from \code{open_pos} onward are unbalanced.}
\usage{
find_matching_paren(text, open_pos)
}
\description{
Find the position of the \verb{)} matching the \code{(} at \code{open_pos} in \code{text},
by scanning forward and tracking parenthesis depth. Handles arbitrary
nesting (e.g. the \verb{)} closing \code{normal(log(0.5), 0.75)}'s outer call).
Returns \code{NA} if the parentheses from \code{open_pos} onward are unbalanced.
}
\keyword{internal}

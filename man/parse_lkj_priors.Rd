% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{parse_lkj_priors}
\alias{parse_lkj_priors}
\title{Turn \verb{cholesky_factor_corr[K] L; ... L ~ lkj_corr_cholesky(eta);} into
one prior row per pairwise correlation.}
\usage{
parse_lkj_priors(
  parameters_block,
  model_block,
  transformed_parameters_block,
  generated_quantities_block,
  data_list,
  pars_filter
)
}
\description{
Unlike every other supported parameter type, the raw elements of a
\code{cholesky_factor_corr[K]} parameter are not individually meaningful:
\code{L[1,1]} is a hard-coded constant 1, and (for \code{K > 2}) the remaining
entries are components of a spherical/stick-breaking parameterisation,
not correlations themselves. The quantities worth plotting are the
off-diagonal entries of the correlation matrix \code{R = L \%*\% t(L)}, each
individually bounded in \verb{(-1, 1)} and following a shifted-Beta marginal
distribution under \code{L ~ lkj_corr_cholesky(eta)} - see \code{?lkjcorr1}.

\code{R} itself has to come from somewhere in the \emph{posterior}, though - a
\code{cholesky_factor_corr[K]} Stan parameter's own draws are \code{L}, not \code{R}.
This looks for the standard Stan idiom for materialising \code{R} from \code{L}:
\verb{corr_matrix[K] Corr_L = multiply_lower_tri_self_transpose(L);} (an
optional leading type/size declaration, in case \code{Corr_L} was already
declared elsewhere and only assigned here), in the \verb{transformed parameters} or \verb{generated quantities} block. If a matching statement is
found, one prior row is emitted per pairwise correlation, named to
match that generated quantity's own draws (e.g. \code{"Corr_L[1,2]"}) so it
flows through \code{PriorPosteriorPlotStan()}'s existing matrix-element
machinery unchanged - \code{Corr_L} is just an ordinary \code{matrix[K,K]}-shaped
generated quantity as far as the rest of the package is concerned. If
no matching statement is found, a warning suggests one to add (skipped
entirely, not warned about, for any \code{L} not in \code{pars_filter}).

Only searches the \verb{transformed parameters}/\verb{generated quantities}
blocks, and only recognises that one idiom - a correlation matrix
declared in the \code{parameters} block instead of computed from \code{L}, or
materialised via some other expression (e.g. a manual \verb{L * L'}), is not
detected.
}
\keyword{internal}

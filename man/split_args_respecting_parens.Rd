% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PriorPosteriorFunctions.R
\name{split_args_respecting_parens}
\alias{split_args_respecting_parens}
\title{Split a Stan sampling-statement argument list on top-level commas only
(i.e. commas not nested inside a further \code{(...)}), so an argument that
is itself a function call with its own comma-separated arguments (e.g.
a 2-argument nested call) is kept intact rather than being split at
that inner comma.}
\usage{
split_args_respecting_parens(args_str)
}
\description{
Split a Stan sampling-statement argument list on top-level commas only
(i.e. commas not nested inside a further \code{(...)}), so an argument that
is itself a function call with its own comma-separated arguments (e.g.
a 2-argument nested call) is kept intact rather than being split at
that inner comma.
}
\keyword{internal}
